import ThemeToggler from "@/components/theme-toggler";

const HomePage = () => {
  return (
    <div className="flex flex-row items-center justify-center w-full h-screen">
      <div>Landing Page / Starting Page!</div>
      <ThemeToggler />
    </div>
  );
};

export default HomePage;
